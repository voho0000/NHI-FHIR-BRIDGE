# NHI-FHIR-BRIDGE 設定檔。複製成 .env 再編輯即可。
# 所有變數都是選填——直接 `docker compose up --build` 就能跑起來。

# ── 安全 (production 部署強烈建議設) ─────────────────────────────────────
# 保護所有寫入 PHI 的端點：
#   POST /sync/upload-structured   POST /sync/upload-html
#   POST /smart/launch-context     POST /fhir/import   GET /fhir/export
# 留空 = 無認證（單機 / 本機開發用）。
# 任何網路可達的部署都應該設一段長隨機字串，例如：
#   python -c 'import secrets; print(secrets.token_urlsafe(32))'
# Chrome Extension 需在 ⚙️ 進階設定 也填入相同的值，並會在 header 帶
# `X-Sync-API-Key: <值>`。
SYNC_API_KEY=

# CORS：除了預設的 localhost dev 端點以外，額外允許的來源（逗號分隔）。
# 例如自架 SMART App / 自架 dashboard 的網域。
#   ALLOW_CORS_ORIGINS=https://your-smart-app.example.com,https://dashboard.example.com
ALLOW_CORS_ORIGINS=

# 對外公開的 FHIR base URL（SMART CapabilityStatement 會用到）
FHIR_BASE_URL=http://localhost:8010/fhir

# ── LLM 備援路徑（選填，預設關閉）─────────────────────────────────────────
# 主要路徑 /sync/upload-structured 不會呼叫 LLM。
# 只有當 NHI 改 JSON API、需要走 HTML 擷取 fallback 才會用到。
#
# - none   ← 預設。fallback 路徑停用；不會送 PHI 到任何第三方。
# - claude ← 把擷取的 HTML 送到 Anthropic Claude API。
# - ollama ← 用你本機 / LAN 上的 Ollama，PHI 不離開內網。
LLM_PROVIDER=none

# 上面選 claude 時填這裡：
ANTHROPIC_API_KEY=

# 上面選 ollama 時填這裡：
# - Docker 環境用 host.docker.internal（Mac/Windows 內建，Linux 用 docker-compose extra_hosts）
# - 本機直跑（不走 Docker）改成 http://localhost:11434
OLLAMA_BASE_URL=http://host.docker.internal:11434
# 推薦模型：
#   qwen2.5vl:7b   ← 預設（vision + 中文 + JSON 輸出俱佳，~5GB）
#   qwen3:8b       ← 純文字、最強 reasoning
OLLAMA_MODEL=qwen2.5vl:7b
