# NHI-FHIR-BRIDGE 設定檔。複製成 .env 再編輯即可。
# 所有變數都是選填——直接 `docker compose up --build` 就能跑起來。

# ── 安全 (production 部署強烈建議設) ─────────────────────────────────────
# 保護所有寫入 PHI 的端點：
#   POST /sync/upload-structured
#   POST /smart/launch-context     POST /fhir/import   GET /fhir/export
# 留空 = 無認證（單機 / 本機開發用）。
# 注意：留空（keyless 模式）時 CORS 只允許 loopback 來源（以及下方
# ALLOW_CORS_ORIGINS / ALLOWED_EXTENSION_IDS 明確列出的來源）——
# 外部 SMART App（含 GitHub Pages demo）要能跨域讀取就必須設 key。
# 任何網路可達的部署都應該設一段長隨機字串，例如：
#   python -c 'import secrets; print(secrets.token_urlsafe(32))'
# Chrome Extension 需在 ⚙️ 進階設定 也填入相同的值，並會在 header 帶
# `X-Sync-API-Key: <值>`。
SYNC_API_KEY=

# CORS：除了預設的 localhost dev 端點以外，額外允許的來源（逗號分隔）。
# 例如自架 SMART App / 自架 dashboard 的網域。
#   ALLOW_CORS_ORIGINS=https://your-smart-app.example.com,https://dashboard.example.com
ALLOW_CORS_ORIGINS=

# Host header 白名單（DNS-rebinding 防護）：loopback（localhost /
# 127.0.0.1 / [::1]）永遠允許；走 reverse proxy 或其他 hostname 連後端時，
# 把額外允許的 host:port 用逗號列在這（docker-compose 已替容器內部
# 通訊設 backend:8010）。Host 不在名單內的請求一律 403。
#   ALLOWED_HOSTS=bridge.internal.example.com:8010
ALLOWED_HOSTS=

# 允許 CORS 的 Chrome extension ID（逗號分隔）。只影響「網頁端」跨域
# 讀取——本專案的 extension 本身不需要 CORS（manifest host_permissions
# 已豁免），所以一般用不到。未設且有設 SYNC_API_KEY 時，fallback 接受
# 任何合法 extension ID（涵蓋 unpacked dev install）；keyless 模式則
# 只接受這裡明確列出的 ID。
#   ALLOWED_EXTENSION_IDS=abcdefghijklmnopabcdefghijklmnop
ALLOWED_EXTENSION_IDS=

# 對外公開的 FHIR base URL（SMART CapabilityStatement 會用到）
FHIR_BASE_URL=http://localhost:8010/fhir

# 開發限定：keyless 模式下，/smart/authorize 沒帶 launch token 時自動選
# 資料庫第一位病人發 code（standalone launch 測試用）。預設關閉 ——
# 任何多人 / 網路可達的部署都不該開。
#   SMART_DEV_AUTOSELECT_PATIENT=1
SMART_DEV_AUTOSELECT_PATIENT=

# This project does NOT integrate with any LLM. Every NHI page-type
# we support is consumed via its stable JSON endpoint directly inside
# the extension; the FHIR mapper is pure deterministic code. PHI never
# leaves your machine, period.
