{
  "name": "nhi-fhir-bridge-extension",
  "version": "0.6.2",
  "private": true,
  "type": "module",
  "description": "Chrome extension: NHI 健康存摺 → FHIR R4 capture (local-only or backend upload).",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@nhi-fhir-bridge/mapper": "*"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "esbuild": "^0.24.0",
    "vitest": "^2.1.0"
  }
}